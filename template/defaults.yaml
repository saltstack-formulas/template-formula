# -*- coding: utf-8 -*-
# vim: ft=yaml
---
template:
  pkg:
    name: template
  rootgroup: root
  config: '/etc/template'
  service:
    name: template
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
